# Introduction to Pytest

## Install Dependencies

```
poetry install
```

## Run the Testing

- Using unittest

  ```
  poetry run python -m unittest discover
  ```

- Using pytest

  ```
  poetry run pytest .
  ```
